<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sin INTERNET no hay sesión ni datos: el arranque no puede completarse. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Recordatorios de racha, anclas y ahorro, y los avisos de apoyo en semáforo rojo. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Visibilidad de la app de teléfono para el protocolo de emergencia (Android 11+). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <!-- Nunca se acepta tráfico sin cifrar: la app habla solo https con su servidor, y
         el historial de recaídas de alguien no puede viajar en claro por una wifi
         pública. -->
    <!-- Sin copia de seguridad automática: aquí hay tokens de sesión y el historial local
         de recaídas y de diario. No puede acabar en el Drive de nadie ni restaurarse en
         otro teléfono sin volver a iniciar sesión. -->
    <application
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Publica los avisos programados. No exportado: solo lo dispara nuestra alarma. -->
        <receiver
            android:name=".avisos.AvisoReceiver"
            android:exported="false" />
    </application>

</manifest>